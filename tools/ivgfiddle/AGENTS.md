# IVG Preview Tooling Formatting

Use the prettifier configuration below for all files in this directory:

```
{
	"useTabs": true,
	"printWidth": 140
}
```

Match the formatting Prettier would output with this profile when editing files here.
