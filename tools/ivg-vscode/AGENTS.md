# VS Code Extension Formatting

Use the `prettifier` configuration below for any UI or script changes in this folder:

```
{
	"useTabs": true,
	"printWidth": 140
}
```

When editing files here, match the output Prettier would generate with that configuration.
